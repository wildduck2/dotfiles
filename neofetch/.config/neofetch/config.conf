# WildDuck Arch - Neofetch Config
# https://github.com/dylanaraps/neofetch/wiki/Customizing-Info

print_info() {
    prin ""
    prin "$(color 6)          ┌─────────────────────────────┐"
    prin "$(color 6)          │  $(color 4) $(color 6) wildduck@arch $(color 6)          │"
    prin "$(color 6)          └─────────────────────────────┘"
    prin ""

    info "$(color 4) " distro
    info "$(color 4) " kernel
    info "$(color 4)󰇄 " model
    info "$(color 4) " uptime
    info "$(color 4) " packages
    info "$(color 4) " shell
    info "$(color 4) " wm
    info "$(color 4) " term
    info "$(color 4) " term_font
    info "$(color 4) " cpu
    info "$(color 4)󰍛 " memory
    info "$(color 4)󰊠 " gpu
    info "$(color 4) " disk
    info "$(color 4)󰍹 " resolution
    prin ""
    info cols
}

# -- Title -----------------------------------------------------------------
title_fqdn="off"

# -- Kernel ----------------------------------------------------------------
kernel_shorthand="on"

# -- Distro ----------------------------------------------------------------
distro_shorthand="off"
os_arch="on"

# -- Uptime ----------------------------------------------------------------
uptime_shorthand="tiny"

# -- Memory ----------------------------------------------------------------
memory_percent="on"
memory_unit="gib"

# -- Packages --------------------------------------------------------------
package_managers="on"

# -- Shell -----------------------------------------------------------------
shell_path="off"
shell_version="on"

# -- CPU -------------------------------------------------------------------
speed_type="bios_limit"
speed_shorthand="on"
cpu_brand="on"
cpu_speed="on"
cpu_cores="logical"
cpu_temp="C"

# -- GPU -------------------------------------------------------------------
gpu_brand="on"
gpu_type="all"

# -- Resolution ------------------------------------------------------------
refresh_rate="on"

# -- Gtk Theme / Icons / Font ----------------------------------------------
gtk_shorthand="off"
gtk2="off"
gtk3="on"

# -- IP Address ------------------------------------------------------------
public_ip_host="http://ident.me"
public_ip_timeout=2

# -- Desktop Environment --------------------------------------------------
de_version="on"

# -- Disk ------------------------------------------------------------------
disk_show=('/')
disk_subtitle="mount"
disk_percent="on"

# -- Song ------------------------------------------------------------------
music_player="auto"
song_format="%artist% - %title%"
song_shorthand="off"
mpc_args=()

# -- Text Colors -----------------------------------------------------------
# Catppuccin Mocha-inspired: title, @, underline, subtitle, colon, info
colors=(4 6 4 6 8 7)

# -- Text Options ----------------------------------------------------------
bold="on"
underline_enabled="on"
underline_char="─"
separator=" ➜ "

# -- Color Blocks ----------------------------------------------------------
block_range=(0 15)
color_blocks="on"
block_width=3
block_height=1
col_offset="auto"

# -- Progress Bars ---------------------------------------------------------
bar_char_elapsed="━"
bar_char_total="─"
bar_border="off"
bar_length=15
bar_color_elapsed="distro"
bar_color_total="distro"
cpu_display="off"
memory_display="off"
battery_display="off"
disk_display="off"

# -- Backend Settings ------------------------------------------------------
image_backend="ascii"
image_source="${HOME}/.config/neofetch/duck_arch.txt"

# -- Ascii Options ---------------------------------------------------------
ascii_distro="auto"
ascii_colors=(4 6 4 6 4 6)
ascii_bold="on"

# -- Image Options ---------------------------------------------------------
image_loop="off"
thumbnail_dir="${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/neofetch"
crop_mode="normal"
crop_offset="center"
image_size="auto"
gap=3
yoffset=0
xoffset=0
background_color=

# -- Misc ------------------------------------------------------------------
stdout="off"
